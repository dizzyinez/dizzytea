import { Component } from "preact"
import { GunCtx } from "./App"
import { useContext } from "preact/hooks"
//import Gun, { IGunInstance } from 'gun';

class Music extends Component {
        const { gun } = useContext(GunCtx);
        render() {
                return ()
        }
}
